# Generate once, store as a Supabase Edge Function secret, and never commit the real value.
# The value must be base64 for exactly 32 random bytes.
LINKED_ACCOUNT_ENCRYPTION_KEY=replace-with-base64-encoded-32-byte-key
